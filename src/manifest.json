{
    "conditions": [
        {
            "any": [
                {"path-exists": "/lib/systemd/system/docker.socket"},
                {"path-exists": "/usr/lib/systemd/system/docker.socket"},
                {"path-exists": "/var/run/docker.sock"}
            ]
        }
    ],
    "menu": {
        "index": {
            "label": "VNCP Manager",
            "order": 50,
            "keywords": [
                {
                    "matches": ["vncp", "docker", "container", "image"]
                }
            ]
        }
    },
    "capabilities": ["service-filtering"]
}
