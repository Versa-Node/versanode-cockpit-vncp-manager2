// Docker doesn't support pods like Podman does, so this is a stub component
export const PodActions = ({ con, onAddNotification, pod, isPodService }) => {
    // Return null since Docker doesn't have pod functionality
    return null;
};