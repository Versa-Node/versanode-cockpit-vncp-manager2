# Please keep this file sorted (LC_COLLATE=C.UTF-8),
# grouped into the 3 categories below:
#  - general patterns (match in all directories)
#  - patterns to match files at the toplevel
#  - patterns to match files in subdirs

# general patterns
*.pyc
*.rpm

# toplevel (/...)
/Test*.html
/Test*.json
/Test*.log
/Test*.log.gz
/Test*.png
/*.whl
/bots
/cockpit-*.tar.xz
/cockpit-docker.spec
/dist/
# /package-lock.json  # Allow committing for Docker version
# /pkg/              # Allow committing build infrastructure
/tmp/
# /tools/            # Allow committing build tools
 
# subdirs (/subdir/...)
/packaging/arch/PKGBUILD
/packaging/debian/changelog
/po/*.pot
 /po/LINGUAS
# /test/common/  # Allow committing essential build utilities
/test/images/
/test/static-code
